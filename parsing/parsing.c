#include "minishell.h"

int	parse(t_shell *data, char *str)
{
    char *str_parse;
    if(ft_strlen(str) == 0)
        return(0);
    if(str_parse == NULL)
        return(0);
    free(str_parse);
    return(0);
}